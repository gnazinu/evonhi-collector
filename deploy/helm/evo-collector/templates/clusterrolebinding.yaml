apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "evo-collector.fullname" . }}
  labels:
    {{- include "evo-collector.labels" . | nindent 4 }}
subjects:
  - kind: ServiceAccount
    name: {{ include "evo-collector.fullname" . }}
    namespace: {{ .Release.Namespace }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "evo-collector.fullname" . }}
